# Examples

Examples are added with the WAVE that implements the corresponding behavior.

The first end-to-end example will demonstrate a local project scan, deterministic resolution, explanation, Claude Code materialization, and lock verification.
