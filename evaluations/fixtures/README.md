# Fixture policy

Fixtures must be synthetic, deterministic, free of real secrets, and small enough for repository review.

Each fixture documents the project signals it is intended to expose. Adversarial fixtures must not be executable outside their dedicated test harness.
