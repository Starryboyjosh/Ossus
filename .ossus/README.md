# Ossus dogfooding configuration

This directory contains checked-in project policy examples intended to become valid Ossus configuration when the corresponding CLI WAVEs are implemented.

Runtime caches, indexes, stores, transactions, and local overrides are ignored by Git.
