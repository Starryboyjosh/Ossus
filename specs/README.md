# Specifications

These files are active product contracts, not examples hidden in the implementation plan.

- `taxonomy/`: governed capabilities, aliases, and deprecations.
- `schemas/`: machine-readable JSON Schemas.
- `examples/`: non-authoritative valid examples.
- `config/`: project and policy examples.

Unknown major schema versions fail closed. External author metadata must never be copied directly into canonical fields.
